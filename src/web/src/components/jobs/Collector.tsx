import { useNavigate } from "react-router-dom";

const Collector = () => {
    const navigate = useNavigate();

    return (
        <div className='collector_menu'>
            
        </div>
    )
}

export default Collector;